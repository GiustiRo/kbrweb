import { DeferLoadingDirective } from './defer-loading.directive';

describe('DeferLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new DeferLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
